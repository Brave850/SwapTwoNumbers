#include <stdio.h>
int main()
{
  int a,b;
  scanf("%d",&a);
  scanf("%d",&b);
  printf("Before swpping two numbers are: %d & %d\n",a,b);
  a=a^b;
  b=a^b;
  a=a^b;
  printf("After swapping two numbers are: %d & %d\n",a,b);
  return 0;
}
